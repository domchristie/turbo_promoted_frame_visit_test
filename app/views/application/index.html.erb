<h1>Index</h1>

<turbo-frame id="frame">
  <p>The following link has a <code>data-turbo-action="advance"</code> attribute and exists within a Turbo Frame. Tapping it should replace the frame's contents, keeping the Index heading, but instead it navigates to the about page with a full reload.</p>
  <%= link_to "About", about_path, data: {turbo_action: :advance} %>
</turbo-frame>
